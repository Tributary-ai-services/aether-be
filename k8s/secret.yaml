apiVersion: v1
kind: Secret
metadata:
  name: aether-backend-secret
  namespace: aether-be
  labels:
    app: aether-backend
type: Opaque
stringData:
  # Neo4j Credentials
  NEO4J_USERNAME: "neo4j"
  NEO4J_PASSWORD: "password"

  # Redis Password (empty for now, shared Redis has no auth)
  REDIS_PASSWORD: ""

  # Keycloak Client Secret
  KEYCLOAK_CLIENT_SECRET: "e78dEfml7xy6YKyHyiQWMMmw7fDs6Kz8"

  # MinIO/S3 Credentials
  AWS_ACCESS_KEY_ID: "minioadmin"
  AWS_SECRET_ACCESS_KEY: "minioadmin123"

  # Router API Key (if needed)
  ROUTER_API_KEY: ""
