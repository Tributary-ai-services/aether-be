# Enforce LF line endings so files stay gofmt-clean regardless of platform.
# CRLF in .go files makes `gofmt -l` flag them, which previously broke CI.
*.go    text eol=lf
*.mod   text eol=lf
*.sum   text eol=lf
*.cypher text eol=lf
*.sh    text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf
