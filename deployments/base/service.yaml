apiVersion: v1
kind: Service
metadata:
  name: aether-backend
  labels:
    app: aether-backend
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: aether-backend