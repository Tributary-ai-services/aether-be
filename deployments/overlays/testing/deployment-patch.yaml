apiVersion: apps/v1
kind: Deployment
metadata:
  name: aether-backend
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: aether-backend
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "300m"