apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aether-backend-ingress
  annotations:
    kubernetes.io/ingress.class: "traefik"
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - aether-staging.aether.com
    - keycloak-staging.aether.com
    secretName: aether-staging-tls
  rules:
  - host: aether-staging.aether.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: aether-backend
            port:
              number: 8080
  - host: keycloak-staging.aether.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: keycloak
            port:
              number: 8080